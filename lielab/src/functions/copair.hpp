#ifndef _LIELAB_FUNCTIONS_COPAIR_HPP
#define _LIELAB_FUNCTIONS_COPAIR_HPP

#include <cassert>

namespace lielab
{
    namespace functions
    {
        // template <lielab::abstract::LieAlgebra LA>
        // double copair(const lielab::domain::cola & mu, const LA & g)
        // {
        //     /*! \f{equation*}{ (\mathfrak{g}^*, \mathfrak{g}) \rightarrow \mathbb{R} \f}
        //     *
        //     * Pairing of vectors on their dual.
        //     */
            
        //     assert(mu._data.size() == g.get_dimension());
        //     return mu._data.dot(g.get_vector());
        // }
    }
}

#endif
